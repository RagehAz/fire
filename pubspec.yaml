name: fire
description: Firebase firestore API.
version: 7.2.0
publish_to: "none"
homepage: https://github.com/RagehAz/fire.git
# --------------------------------------------------------------------------------
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"
# --------------------------------------------------------------------------------
dependencies:
  flutter:
    sdk: flutter
  # ------------------------------------
  basics:
    # -----------------
    # IOS
#    path: /Users/rageh/projects/bldrs/basics
    # ----------------
    # ANDROID
    path: E:/dev/projects/bldrs_packs/basics
    # ----------------
    # GIT
#    git:
#      url: https://github.com/RagehAz/basics.git
  # ------------------------------------
  extension_google_sign_in_as_googleapis_auth: 2.0.13
  flutter_facebook_auth: 7.2.0
  google_sign_in: 6.3.0
  firebase_core: 4.12.1
  googleapis_auth: 2.3.3
  firebase_auth: 6.5.6
  firebase_database: 12.4.6
  firebase_storage: 13.4.5
  sign_in_with_apple: 8.1.0
  firebase_ui_auth: 3.1.0
  firebase_ui_oauth_facebook: 2.1.0
  firebase_ui_oauth_apple: 2.1.0
  firebase_ui_oauth_google: 2.1.0
# --------------------------------------------------------------------------------
dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.8.0
# --------------------------------------------------------------------------------
dependency_overrides:
  # firebase_ui_auth pins email_validator ^2.1.17 (stale constraint, no breaking
  # API change since) while basics requires ^3.0.0 — force the newer one.
  email_validator: ^3.0.0
# --------------------------------------------------------------------------------
flutter:
# --------------------------------------------------------------------------------
